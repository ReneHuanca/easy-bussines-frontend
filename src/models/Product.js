class Product {

    constructor(name, quantity, unit_price, total_in_stock, category_id) {
        this.name = name,
        this.quantity = quantity;
        this.unit_price = unit_price;
        this.total_in_stock = total_in_stock;
        this.category_id = category_id;
    }

}

export default Product;